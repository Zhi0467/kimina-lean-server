"""Utilities for the Pantograph `/exec` HTTP benchmark.

Kept separate from the `examples/pantograph_step_benchmark.py` entry point so the
mining, replay, and metrics helpers can be unit-tested without a running server.
"""
